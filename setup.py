from setuptools import setup, find_packages

setup(
    name='OHIOgrapher',
    version='0.1',
    description='creates a graph, kinda janky method',
    author='André Glycerio',
    packages=find_packages(),
)